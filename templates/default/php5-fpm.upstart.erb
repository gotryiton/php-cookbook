description "php5-fpm upstart script"

start on filesystem
stop on runlevel [!2345]

respawn
respawn limit 15 5

exec <%= node[:php][:fpm][:src_binary] %> -c <%= node[:php][:fpm][:conf_dir] %>/php.ini -y <%= node[:php][:fpm][:conf_dir] %>/php-fpm.conf
